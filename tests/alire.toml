name = "tests"
description = "test suite for version"
version = "0.1.0-dev"

authors = ["Bent Bracke"]
maintainers = ["Bent Bracke <bent@bracke.dk>"]
maintainers-logins = ["bracke"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
website = ""
tags = []


executables = ["tests"]

[[depends-on]]
version = "*"

[[depends-on]]
gnat_native = "=15.2.1"

[[depends-on]]
aunit = "^26.0.0"

# Shared tooling helpers used by the test support (tooling only).
[[depends-on]]
project_tools = "~0.1.0"

[[pins]]
version = { path='..' }

[[pins]]
project_tools = { path = "../../project_tools" }
